"""
🎓 بوت أسئلة مادة أنشطة الإدارة في الإسلام - النسخة الاحترافية المتكاملة
═══════════════════════════════════════════════════════════════════════════════
✨ المميزات:
• واجهة مستخدم تفاعلية متطورة
• نظام إدارة أسئلة مرن وسهل التحديث
• دعم أنواع متعددة من الأسئلة (اختيار من متعدد، صح/خطأ، أكمل الفراغ)
• مؤقت ذكي مع شريط تقدم
• إحصائيات تفصيلية
• نظام تقييم ذكي
═══════════════════════════════════════════════════════════════════════════════
"""

import logging
import os
import asyncio
from datetime import datetime
from collections import defaultdict
from typing import List, Dict, Any, Optional
from telegram import Update, InlineKeyboardButton, InlineKeyboardMarkup
from telegram.ext import (
    Application, CommandHandler, CallbackQueryHandler, 
    ContextTypes, ConversationHandler
)

# ═══════════════════════════════════════════════════════════════════════════════
# 🔧 إعدادات النظام
# ═══════════════════════════════════════════════════════════════════════════════

logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.INFO
)

# 🔑 التوكن (يجب تغييره في الإنتاج)
TOKEN = os.environ.get('BOT_TOKEN', '8550588818:AAHkdtokih3ndkVHYNEEMo__8mKBQsg1tH0')

# ⏱️ الإعدادات الزمنية
TIME_LIMIT = 40  # ثواني لكل سؤال
WARNING_TIME = 10  # تحذير عند 10 ثواني متبقية

# 🎨 نظام الألوان والرموز
class Icons:
    """مكتبة الأيقونات والرموز"""
    # ألوان
    GOLD = '🟡'
    BLUE = '🔵'
    GREEN = '🟢'
    RED = '🔴'
    PURPLE = '🟣'
    ORANGE = '🟠'
    YELLOW = '🟡'
    
    # رموز الواجهة
    BOT = '🤖'
    BOOK = '📚'
    TEST = '📝'
    TIMER = '⏱️'
    CHART = '📊'
    TROPHY = '🏆'
    STAR = '⭐'
    CHECK = '✅'
    CROSS = '❌'
    ARROW = '➡️'
    BACK = '🔙'
    HOME = '🏠'
    INFO = 'ℹ️'
    PLAY = '▶️'
    PAUSE = '⏸️'
    STOP = '⏹️'
    ALARM = '⏰'
    BELL = '🔔'
    FIRE = '🔥'
    TARGET = '🎯'
    MEDAL = '🥇'
    LOCK = '🔒'
    UNLOCK = '🔓'
    SAVE = '💾'
    REFRESH = '🔄'
    SEARCH = '🔍'
    LIGHT = '💡'
    WARNING = '⚠️'
    ERROR = '❗'
    SUCCESS = '✨'
    HEART = '❤️'
    SPARKLES = '✨'

# ═══════════════════════════════════════════════════════════════════════════════
# 📦 نظام إدارة الأسئلة (منفصل للسهولة)
# ═══════════════════════════════════════════════════════════════════════════════

class QuestionManager:
    """مدير الأسئلة - يتيح إضافة أسئلة بسهولة"""
    
    def __init__(self):
        self.tests = {
            'test1': {
                'name': 'الاختبار الأول',
                'description': 'الدروس 7-10 والتكميلية',
                'icon': Icons.GOLD,
                'questions': []
            },
            'test2': {
                'name': 'الاختبار الثاني', 
                'description': 'أسئلة العهد كاملة',
                'icon': Icons.PURPLE,
                'questions': []
            },
            'test3': {
                'name': 'اختبار الفراغات',
                'description': 'أكمل الفراغ من الأسئلة',
                'icon': Icons.BLUE,
                'questions': []
            }
        }
        self._load_all_questions()
    
    # ─────────────────────────────────────────────────────────────────────────
    # 📝 دوال إضافة الأسئلة (للاستخدام عند إضافة أسئلة جديدة)
    # ─────────────────────────────────────────────────────────────────────────
    
    def add_multiple_choice(self, test_id: str, question: str, 
                          options: List[str], correct: str, 
                          explanation: str = "") -> None:
        """إضافة سؤال اختيار من متعدد"""
        self.tests[test_id]['questions'].append({
            'type': 'multiple_choice',
            'question': question,
            'options': options,
            'correct': correct,
            'explanation': explanation
        })
    
    def add_true_false(self, test_id: str, question: str, 
                      correct: bool, explanation: str = "") -> None:
        """إضافة سؤال صح/خطأ"""
        self.tests[test_id]['questions'].append({
            'type': 'true_false',
            'question': question,
            'correct': 'صحيح' if correct else 'خطأ',
            'explanation': explanation
        })
    
    def add_fill_blank(self, test_id: str, question: str, 
                      answer: str, explanation: str = "") -> None:
        """إضافة سؤال أكمل الفراغ"""
        self.tests[test_id]['questions'].append({
            'type': 'fill_blank',
            'question': question,
            'correct': answer,
            'explanation': explanation
        })
    
    # ─────────────────────────────────────────────────────────────────────────
    # 📚 تحميل الأسئلة (يتم تعديل هذا الجزء فقط عند إضافة أسئلة)
    # ─────────────────────────────────────────────────────────────────────────
    
    def _load_all_questions(self):
        """تحميل جميع الأسئلة"""
        self._load_test1_questions()
        self._load_test2_questions()
        self._load_test3_fill_blank()
    
    def _load_test1_questions(self):
        """الاختبار الأول: الدروس 7-10 والتكميلية"""
        t = 'test1'
        
        # ═════════════════════════════════════════════════════════════════
        # الدرس 7 و 8 - اختيار من متعدد
        # ═════════════════════════════════════════════════════════════════
        
        self.add_multiple_choice(t,
            "1- قد يساهم المجتمع في اختلال عمل الجهاز الإداري للدولة من خلال:",
            ["أ- عدم تفهمه لأهمية توفر الكفاءة", 
             "ب- فرضه شخصيات غير كفؤة في مناصب معينة", 
             "ج- كل (أ) و (ب) صحيح", 
             "د- لا شيء مما سبق"],
            "ج",
            "الكفاءة شرط أساسي لنجاح العمل الإداري"
        )
        
        self.add_multiple_choice(t,
            "2- كل التعيينات للمسؤولين المبنية على الميل والمجاملة فقط، وليس على الكفاءة العملية، فيه:",
            ["أ- غير نافذة", "ب- غير مناسبة", "ج- غير جائزة", "د- كل ما سبق"],
            "ج",
            "التعيين بالمحاباة ظلم للمجتمع وخيانة للأمانة"
        )
        
        self.add_multiple_choice(t,
            "3- التعيين في المناصب بالمحاباة والأثرة يعتبر خيانة:",
            ["أ- لله سبحانه وتعالى", "ب- للناس وللأمة", "ج- كل ما سبق صحيح", "د- لا شيء مما سبق"],
            "ج",
            "الخيانة تشمل حقوق الله وحقوق العباد"
        )
        
        self.add_multiple_choice(t,
            "4- قول الإمام علي (ع) (من أهل البيوتات الصالحة) يعني أن يكون:",
            ["أ- من أهل الأنساب والأحساب", 
             "ب- ممن ترّبوا على مكارم الأخلاق والقيم الفاضلة", 
             "ج- من الأغنياء", "د- من العلماء فقط"],
            "ب",
            "البيئة الصالحة تؤثر إيجاباً في تكوين الشخصية"
        )
        
        self.add_multiple_choice(t,
            "5- من أهم ما يجب ملاحظته عند الرقابة السرية على المسؤول واختباره:",
            ["أ- حسن علاقاته الشخصية", 
             "ب- أداؤه أمانة المسؤولية ورفقه بالناس", 
             "ج- انضباطه في الدوام اليومي", "د- مظهره الخارجي"],
            "ب",
            "الأمانة والرفق من أهم صفات المسؤول الناجح"
        )
        
        self.add_multiple_choice(t,
            "6- قد يتغير بعض المسؤولين ممن كان ظاهرهم الصالح بسبب:",
            ["أ- إصابته بالغرور والعجب والكبر", 
             "ب- مواجهته واقعا جديدا مغريا", 
             "ج- كل ما سبق صحيح", "د- كثرة الانتقادات"],
            "ج",
            "الإنسان قد يتغير بتغير الظروف والامتحانات"
        )
        
        self.add_multiple_choice(t,
            "7- أكثر ما تكون خيانات المسؤولين في:",
            ["أ- المال والإمكانات", "ب- التآمر مع الأعداء", 
             "ج- ظلم الناس", "د- إفشاء الأسرار"],
            "أ",
            "المال والسلطة أكبر محاكم للنفوس"
        )
        
        self.add_multiple_choice(t,
            "8- تراجع اهتمام المسلمين بالزراعة بسبب:",
            ["أ- سياسات الأعداء التي ينفذها الحكام العملاء", 
             "ب- الغفلة والتخلف اللذين سادا قرونا من الزمن", 
             "ج- كل ما سبق صحيح", "د- قلة الأراضي"],
            "ج",
            "الاستعمار والتخلف من أسباب تخلف الزراعة"
        )
        
        self.add_multiple_choice(t,
            "9- مما يجعل الناس يستفيدون بشكل أكبر من المحاصيل الزراعية:",
            ["أ- الصناعة التحويلية", "ب- إنتاج البذور والمشاتل", 
             "ج- تصنيع الحراثات والحصادات", "د- استيراد المحاصيل"],
            "أ",
            "الصناعة التحويلية تضيف قيمة للمنتج الزراعي"
        )
        
        self.add_multiple_choice(t,
            "10- إراحة المزارعين والإجمام لهم سيفيد الدولة من حيث:",
            ["أ- اكتساب ثقتهم", "ب- يكونون لها سندا في الظروف الصعبة", 
             "ج- كل ما سبق صحيح", "د- زيادة الضرائب"],
            "ج",
            "رعاية المزارعين استثمار في استقرار الدولة"
        )
        
        self.add_multiple_choice(t,
            "11- أحيانا يشعر المزارع أنه محارب من بعض المسؤولين عندما:",
            ["أ- يفرضون مزيدا من الضرائب على الزراعة", 
             "ب- يُكثرون من مضايقاته وفرض الغرامات عليه", 
             "ج- كل ما سبق صحيح", "د- يقدمون له المساعدة"],
            "ج",
            "الظلم الإداري يحول المزارع إلى عدو"
        )
        
        self.add_multiple_choice(t,
            "12- من مصاديق قول الإمام علي (ع): (فإن العمران مُحتمل ما حملته):",
            ["أ- البنية الاقتصادية القوية تمثل سندا للبلد في مواجهة التحديات", 
             "ب- عمران المدن بالأعداد الكبيرة من الأبنية", 
             "ج- كثرة السكان", "د- كل ما سبق"],
            "أ",
            "العمران الحقيقي هو القوة الاقتصادية والبشرية"
        )
        
        # ═════════════════════════════════════════════════════════════════
        # الدرس 7 و 8 - صح وخطأ
        # ═════════════════════════════════════════════════════════════════
        
        self.add_true_false(t,
            "13- يُعتبر محافظ المحافظة ومدير المديرية من (العمال) الذين ذكرهم الإمام علي (ع) في قوله (ثم انظر في أمور عمالك).",
            True,
            "العمال هم المسؤولون التنفيذيون في الدولة"
        )
        
        self.add_true_false(t,
            "14- التعيين للمسؤولين، بناءً على المحاباة والأثرة جريمة كبيرة، ولكنه لا يُعطل البناء الحضاري للأمة.",
            False,
            "التعيين بالمحاباة يعطل البناء الحضاري ويفسد المجتمع"
        )
        
        self.add_true_false(t,
            "15- يُفهم من كلام الإمام علي (ع) أنه يجب اختبار المسؤولين في كفاءتهم بعد تعيينهم في مسؤولياتهم.",
            False,
            "الاختبار يكون قبل التعيين وليس بعده"
        )
        
        self.add_true_false(t,
            "16- الرقابة السرية على المسؤولين تحملهم على الرفق بالرعية.",
            True,
            "الرقابة تضمن حسن الأداء والتعامل"
        )
        
        self.add_true_false(t,
            "17- يستحق الخائن أن يشهر به حتى لا يُخدع به الآخرون فيعتمدوا عليه في عمل جديد.",
            True,
            "التشهير بالخائن وقاية للمجتمع منه"
        )
        
        self.add_true_false(t,
            "18- نجحت كثير من الشعوب والأمم من غير العرب والمسلمين في النهضة الزراعية؛ لأنهم اعتبروها مجرد مورد من موارد الاقتصاد فقط.",
            False,
            "النهضة الزراعية تحتاج إلى رؤية شاملة وليس اقتصاداً فقط"
        )
        
        self.add_true_false(t,
            "19- ذكر السيد القائد في الدرس أن من الحلول لضبط كلفة المنتج الزراعي: الاستفادة من تجارب بلدان العالم التي تنتج بكلفة أقل.",
            True,
            "الاستفادة من التجارب العالمية ضرورة للتطوير"
        )
        
        self.add_true_false(t,
            "20- كان التعليم الزراعي في المراحل الماضية متوسطا لا يرقى إلى المستوى اللازم.",
            True,
            "التعليم الزراعي كان بحاجة للتطوير"
        )
        
        self.add_true_false(t,
            "21- تتدهور العملية الزراعية وتنتهي أكثر المزارع حين يوضع أهلها تحت سياسة الإفقار والغرامات الظالمة.",
            True,
            "الظلم الإداري يدمر الزراعة"
        )
        
        # ═════════════════════════════════════════════════════════════════
        # الدرس 9 و 10 - اختيار من متعدد
        # ═════════════════════════════════════════════════════════════════
        
        self.add_multiple_choice(t,
            "22- دور الكتاب (مسؤولي المكاتب) مهم جدا؛ لأنهم:",
            ["أ- يمثلون حلقة الوصل بين المسؤول والمجتمع", 
             "ب- يباشرون إنجاز المعاملات وكثيرا من الترتيبات", 
             "ج- كل ما سبق صحيح", "د- لا شيء مما سبق"],
            "ج",
            "المكاتب هي عصب العمل الإداري"
        )
        
        self.add_multiple_choice(t,
            "23- من أبرز ما عُرف عن المكاتب الحكومية في معظم البلدان العربية:",
            ["أ- تأخير معاملات الناس إلى حد كبير", 
             "ب- إنجاز معاملات الناس إلى حد متوسط", 
             "ج- سرعة الإنجاز", "د- العدالة"],
            "أ",
            "البطء الإداري من مشاكل المكاتب الحكومية"
        )
        
        self.add_multiple_choice(t,
            "24- لا يصلح أن يكون في مسؤولي المكاتب من إذا غضب أضرّ بعمله؛ لأنه في هذه الحالة لم يتوفر فيه معيار:",
            ["أ- أجمعه لوجوه صالح الأخلاق", "ب- لا تبطره الكرامة", 
             "ج- لا تقصر به الغفلة", "د- كل ما سبق"],
            "أ",
            "صالح الأخلاق يضبط الانفعالات"
        )
        
        self.add_multiple_choice(t,
            "25- شخص واع وفاهم للعمل، ويجيد الإصدار والإيراد، ويغلق على حاسوبه الذي يحتوي على بعض الأسرار المهمة في سيارته عند دخوله إلى المطعم:",
            ["أ- يجوز استمراره في مكتب الوزير", 
             "ب- يجب استبعاده من مكتب الوزير", 
             "ج- يجب استبعاده إذا تكررت", "د- لا بأس في ذلك"],
            "ب",
            "التساهل في الأسرار خطأ جسيم"
        )
        
        self.add_multiple_choice(t,
            "26- من صفات من (تبطره الكرامة) أنه:",
            ["أ- معجب بنفسه مغرور", "ب- يتصور أنه الأجدر بالمسؤولية", 
             "ج- كل ما سبق صحيح", "د- متواضع"],
            "ج",
            "التبطر نتيجة الغرور والكبر"
        )
        
        self.add_multiple_choice(t,
            "27- أطلقت العرب على من يتمرد على من يكرمه صفة:",
            ["أ- النمام", "ب- الواشي", "ج- اللئيم", "د- الكريم"],
            "ج",
            "اللئيم من يقابل الإحسان بالإساءة"
        )
        
        self.add_multiple_choice(t,
            "28- قول الإمام عليه السلام (لا يضعف عقدا اعتقده لك ولا يعجز عن إطلاق ما عقد عليك) يعبر به عن أنه:",
            ["أ- حصيف متقن لعمله", "ب- أمين لا يخون", 
             "ج- منظم ودقيق", "د- ذكي"],
            "ب",
            "الأمانة في الوفاء بالعقود"
        )
        
        self.add_multiple_choice(t,
            "29- من يتم توليتهم في مسؤوليات مهمة لأجل صلاح آبائهم، فيه مخالفة لعهد الإمام عليه السلام من حيث:",
            ["أ- الاكتفاء بصلاح آبائهم وعدم اختبارهم هم", 
             "ب- عدم اختبارهم من خلال تجاربهم السابقة", 
             "ج- كل ما سبق", "د- لا شيء مما سبق"],
            "أ",
            "الصلاح ينتقل بالتربية لا بالوراثة"
        )
        
        self.add_multiple_choice(t,
            "30- يراعى في اختبار المرشح لمسؤول في المكاتب مستوى:",
            ["أ- إنجازاته الحسنة السابقة المتعلقة بعامة الناس", 
             "ب- نجاحاته الإدارية السابقة بفئة خاصة", 
             "ج- اجتيازه الاختبار التحريري", "د- كل ما سبق"],
            "أ",
            "النجاح مع عامة الناس أهم من النجاح مع الخاصة"
        )
        
        # ... (يمكن إضافة المزيد من الأسئلة بنفس النمط)
        
        # ═════════════════════════════════════════════════════════════════
        # الدروس التكميلية - اختيار من متعدد
        # ═════════════════════════════════════════════════════════════════
        
        self.add_multiple_choice(t,
            "57- أحد الخيارات التالية لا يصدق على قول الإمام: (وأنح عنهم الضيق والأنف):",
            ["أ- لا تستمع لهم وأنت في حالة ضيق", 
             "ب- لا تأنف من القعود معهم والاستماع إليهم", 
             "ج- تحمل من قد تصدر منهم الإساءة", "د- كل ما سبق"],
            "أ",
            "الإمام يأمر بالصبر لا بالانفعال"
        )
        
        self.add_multiple_choice(t,
            "58- العبارة الخطأ بين الخيارات التالية هي:",
            ["أ- اختر أفضل الأوقات للقربة إلى الله", 
             "ب- أعمال مسؤوليتك عبادة لله إذا صلحت نيتك", 
             "ج- خصص وقتا للصلاة أكثر من وقت مسؤوليتك", "د- كل ما سبق صحيح"],
            "ج",
            "العدل بين العبادة والمسؤولية مطلوب"
        )
        
        self.add_multiple_choice(t,
            "59- أحد الخيارات التالية ليس من النتائج الكارثية لاحتجاب المسؤول عن الناس:",
            ["أ- تنقلب صورة الأمور والقضايا عنده", 
             "ب- ينجح العدو في التشويش على قراراته", 
             "ج- لا يؤثر عليه الجلساء", "د- كل ما سبق"],
            "ج",
            "الاحتجاب يؤثر سلباً على قرارات المسؤول"
        )
        
        # ═════════════════════════════════════════════════════════════════
        # الدروس التكميلية - صح وخطأ
        # ═════════════════════════════════════════════════════════════════
        
        self.add_true_false(t,
            "69- عبر الإمام علي ب(الطبقة السفلى) باعتبار ما هم عليه من تصنيف المجتمع لهم.",
            False,
            "الإمام عبر عنهم باحترام رغم ظروفهم"
        )
        
        self.add_true_false(t,
            "70- ينبغي وضع احتياجات الفقراء من الأولويات الأساسية حتى في أيام الحرب.",
            True,
            "رعاية الفقراء ضرورة دائمة"
        )
        
        self.add_true_false(t,
            "71- (فلا تشخصن همك عنهم) معناه: اجعلهم من ضمن أولوياتك.",
            True,
            "عدم إهمال الفقراء والمحتاجين"
        )
        
        self.add_true_false(t,
            "79- رسول الله أرسل الإمام عليا إلى اليمن مرتين.",
            True,
            "أرسله مرتين للدعوة والتعليم"
        )
        
        self.add_true_false(t,
            "80- لا يؤثر احتجاب المسؤول على نفسيته سلبا وإنما يؤثر على دينه.",
            False,
            "الاحتجاب يؤثر على النفسية والأداء"
        )
        
        self.add_true_false(t,
            "81- لا يوجد في عهد الإمام ما يمنع من منح المسؤول لأقاربه سيارات جديدة.",
            False,
            "الإمام نهى عن التبذير والإسراف"
        )
    
    def _load_test2_questions(self):
        """الاختبار الثاني: أسئلة العهد كاملة"""
        t = 'test2'
        
        self.add_multiple_choice(t,
            "1- ماذا يعني قوله ﷺ: \"فَاسْتَعْمِلْهُمُ اخْتِبَاراً\" في سياق اختيار العمال؟",
            ["أ- أن توليهم بناءً على المحاباة والأثرة", 
             "ب- أن تختبرهم وتختبر كفاءتهم قبل التولية", 
             "ج- أن تتركهم يعملون دون أي متابعة", 
             "د- أن تقدم الأقارب والأصدقاء على غيرهم"],
            "ب",
            "الاختبار قبل التولية ضرورة"
        )
        
        self.add_multiple_choice(t,
            "2- ما معنى \"المحاباة\" في التولية كما ورد في النص؟",
            ["أ- تولية الشخص لكفاءته العلمية والعملية", 
             "ب- تولية الشخص لقرابته أو صداقته فقط", 
             "ج- تولية الشخص بعد اجتيازه اختبارًا عمليًا", 
             "د- تولية الشخص بناءً على مشورة أهل الخبرة"],
            "ب",
            "المحاباة هي التولية بدون كفاءة"
        )
        
        self.add_multiple_choice(t,
            "3- ما نتيجة التولية بالمحاباة والأثرة؟",
            ["أ- تحقيق العدل والمساواة بين الناس", 
             "ب- جمع شعب الجور والخيانة", 
             "ج- زيادة الإنتاج وتحسين الأداء", 
             "د- تقوية الدولة واستقرارها"],
            "ب",
            "المحاباة تؤدي للفساد والظلم"
        )
        
        self.add_multiple_choice(t,
            "4- \"فَإِنَّهُمَا جَمَاعٌ مِنْ شُعَبِ الْجُورِ وَالْخِيَانَةِ\" يشير الضمير \"هما\" إلى:",
            ["أ- الاختبار والتجربة", "ب- الكفاءة والأمانة", 
             "ج- المحاباة والأثرة", "د- العدل والإنصاف"],
            "ج",
            "المحاباة والأثرة منبوذتان"
        )
        
        self.add_multiple_choice(t,
            "5- من هم \"أهل التجربة والحياء\" الذين أمر الإمام علي ﷺ بتوخيهم للعمالة؟",
            ["أ- من لهم تجربة عملية فقط", 
             "ب- من يجمعون بين التجربة العملية والحياء والأخلاق", 
             "ج- من لهم قرابة من الوالي", "د- من لهم قدم في الإسلام فقط"],
            "ب",
            "الجمع بين الكفاءة والأخلاق مطلوب"
        )
        
        self.add_multiple_choice(t,
            "6- ما أهمية صفة \"الحياء\" فيمن يختار للعمل؟",
            ["أ- لأنه يزيد من جرأته على الخيانة", 
             "ب- لأنه يمنعه من الإقدام على الفساد والتفريط", 
             "ج- لأنه يجعله قاسيًا في التعامل", 
             "د- لأنه يجعله غير قادر على اتخاذ القرارات"],
            "ب",
            "الحياء يحفظ من الفساد"
        )
        
        self.add_multiple_choice(t,
            "7- \"مِنْ أَهْلِ الْبُيُوتَاتِ الصَّالِحَةِ\" تعني:",
            ["أ- من ينتمون لعائلات غنية فقط", 
             "ب- من تربوا على مكارم الأخلاق والقيم الصالحة", 
             "ج- من سكنوا بيوتًا كبيرة", "د- من لهم نفوذ اجتماعي فقط"],
            "ب",
            "البيئة الصالحة تربي الأجيال"
        )
        
        self.add_multiple_choice(t,
            "8- ما فائدة الاعتماد على ذوي البيوتات الصالحة والسوابق الحسنة في المسؤوليات؟",
            ["أ- لأنهم أكثر ثراءً", 
             "ب- لأن الأخلاق والقيم أصبحت أصيلة فيهم وسهلة الالتزام بها", 
             "ج- لأنهم أقل خبرة", "د- لأنهم أكثر طمعًا في المنصب"],
            "ب",
            "التربية الصالحة تظهر في السلوك"
        )
        
        self.add_multiple_choice(t,
            "13- \"ثُمَّ أَسْبِغْ عَلَيْهِمُ الرِّزْقَ\" ما المقصود بهذا؟",
            ["أ- أن يكثر عليهم المطر", 
             "ب- أن يعطيهم أرزاقًا ومرتبات مجزية تكفيهم", 
             "ج- أن يمنع عنهم الرزق", "د- أن يكلفهم بأعمال شاقة"],
            "ب",
            "الرزق الكافي يحفظ الأمانة"
        )
        
        self.add_multiple_choice(t,
            "14- ما الحكمة من إسباغ الرزق على العمال والمسؤولين؟",
            ["أ- ليتكبروا على الناس", 
             "ب- ليكون قوة لهم على استصلاح أنفسهم وغنًى لهم عن تناول ما تحت أيديهم", 
             "ج- ليدخروا المال لأنفسهم فقط", "د- ليتركوا العمل"],
            "ب",
            "الرزق يقوي على الصلاح ويغني عن الخيانة"
        )
        
        self.add_multiple_choice(t,
            "17- \"ثُمَّ تَفَقَّدْ أَعْمَالَهُمْ\" ما أهمية تفقد أعمال العمال والمسؤولين؟",
            ["أ- لإذلالهم وتعذيبهم", 
             "ب- لتصويب الأخطاء ومعرفة مدى نجاحهم وإصلاح الخلل", 
             "ج- للتجسس عليهم فقط", "د- لجمع الأموال منهم"],
            "ب",
            "التفقد للتصحيح والتطوير"
        )
        
        self.add_multiple_choice(t,
            "19- ما هو \"انْتِعَاثُ الْعُيُونِ\" الذي أمر به الإمام علي ﷺ؟",
            ["أ- إرسال الجواسيس لقتل الناس", 
             "ب- إرسال مراقبين سريين من أهل الصدق والوفاء لمتابعة الأعمال", 
             "ج- إغلاق العيون ومنع الرقابة", "د- جمع الأموال من العيون"],
            "ب",
            "الرقابة السرية من أدوات العدل"
        )
        
        self.add_multiple_choice(t,
            "22- \"وَتَحَفَّظْ مِنَ الْأَعْوَانِ\" ماذا تعني هذه العبارة؟",
            ["أ- الثقة العمياء في الأعوان", 
             "ب- التيقظ والانتباه للأعوان وعدم الغفلة عن أدائهم", 
             "ج- إهمال متابعتهم تمامًا", "د- تقديمهم على غيرهم بلا سبب"],
            "ب",
            "التحفظ ضرورة حتى مع الصالحين"
        )
        
        self.add_multiple_choice(t,
            "24- ماذا يفعل الوالي إذا ثبتت خيانة أحد الأعوان بعد اجتماع أخبار العيون بذلك؟",
            ["أ- يتجاهل الأمر", 
             "ب- يبسط عليه العقوبة ويأخذه بما أصاب من عمل ويشهر به", 
             "ج- يزيد في راتبه", "د- يتركه يستمر في منصبه"],
            "ب",
            "العقوبة والتشهير رادع للخائن"
        )
    
    def _load_test3_fill_blank(self):
        """اختبار أكمل الفراغات - يُنشأ تلقائياً من الأسئلة السابقة"""
        t = 'test3'
        
        # يتم إنشاء أسئلة الفراغات من الأسئلة المهمة
        fill_blank_data = [
            ("قال الإمام علي (ع): (ثم انظر في أمور ______)", "عمالك"),
            ("التعيين في المناصب بالمحاباة يعتبر ______ لله وللأمة", "خيانة"),
            ("من أهم صفات المسؤول: ______ المسؤولية ورفقه بالناس", "أداؤه أمانة"),
            ("الرقابة ______ على المسؤولين أكثر فاعلية من العلنية", "السرية"),
            ("إراحة ______ والإجمام لهم سيفيد الدولة", "المزارعين"),
            ("دور ______ (مسؤولي المكاتب) مهم جداً", "الكتاب"),
            ("قال الإمام: (لا يضعف ______ اعتقده لك)", "عقداً"),
            ("______ الغليظ يؤثر سلباً على الحركة التجارية", "المسؤول الفظ"),
            ("قال الإمام: (وأنح عنهم ______ والأنف)", "الضيق"),
            ("العدو إذا دعا للصلح فيجب قبول ما دام ______ فيه رضا", "الله"),
            ("فاستعملهم ______", "اختباراً"),
            ("من أهل ______ الصالحة", "البيوتات"),
            ("أسبغ عليهم ______", "الرزق"),
            ("ثم تفقد ______", "أعمالهم"),
            ("انتعاث ______", "العيون"),
        ]
        
        for question, answer in fill_blank_data:
            self.add_fill_blank(t, question, answer)
    
    def get_test(self, test_id: str) -> Dict[str, Any]:
        """الحصول على اختبار معين"""
        return self.tests.get(test_id, {})
    
    def get_all_tests(self) -> Dict[str, Dict[str, Any]]:
        """الحصول على جميع الاختبارات"""
        return self.tests

# إنشاء مدير الأسئلة
question_manager = QuestionManager()

# ═══════════════════════════════════════════════════════════════════════════════
# 📊 نظام الإحصائيات والمستخدمين
# ═══════════════════════════════════════════════════════════════════════════════

class UserStats:
    """نظام إدارة إحصائيات المستخدمين"""
    
    def __init__(self):
        self.stats = defaultdict(lambda: {
            'tests': {},
            'total_attempts': 0,
            'best_scores': {},
            'last_activity': None
        })
    
    def record_attempt(self, user_id: int, test_id: str, score: int, total: int):
        """تسجيل محاولة جديدة"""
        user_data = self.stats[user_id]
        
        if test_id not in user_data['tests']:
            user_data['tests'][test_id] = {
                'attempts': [],
                'best_score': 0,
                'average': 0
            }
        
        percentage = (score / total) * 100 if total > 0 else 0
        
        attempt = {
            'date': datetime.now().strftime("%Y-%m-%d %H:%M"),
            'score': score,
            'total': total,
            'percentage': percentage
        }
        
        user_data['tests'][test_id]['attempts'].append(attempt)
        user_data['total_attempts'] += 1
        user_data['last_activity'] = datetime.now()
        
        # تحديث أفضل نتيجة
        if percentage > user_data['tests'][test_id].get('best_score', 0):
            user_data['tests'][test_id]['best_score'] = percentage
        
        # تحديث المعدل
        attempts = user_data['tests'][test_id]['attempts']
        avg = sum(a['percentage'] for a in attempts) / len(attempts)
        user_data['tests'][test_id]['average'] = avg
    
    def get_user_stats(self, user_id: int) -> Dict[str, Any]:
        """الحصول على إحصائيات المستخدم"""
        return dict(self.stats[user_id])
    
    def get_leaderboard(self, limit: int = 10) -> List[tuple]:
        """الحصول على لوحة المتصدرين"""
        leaderboard = []
        for user_id, data in self.stats.items():
            total_score = sum(
                t.get('best_score', 0) 
                for t in data['tests'].values()
            )
            leaderboard.append((user_id, total_score, data['total_attempts']))
        
        leaderboard.sort(key=lambda x: x[1], reverse=True)
        return leaderboard[:limit]

# إنشاء نظام الإحصائيات
user_stats = UserStats()

# ═══════════════════════════════════════════════════════════════════════════════
# 🎨 نظام الواجهة والتصميم
# ═══════════════════════════════════════════════════════════════════════════════

class UI:
    """مكتبة عناصر الواجهة"""
    
    @staticmethod
    def header(title: str, icon: str = "") -> str:
        """إنشاء رأس الصفحة"""
        return (
            f"╔══════════════════════════════════╗\n"
            f"║  {icon} **{title}**  {icon}  ║\n"
            f"╚══════════════════════════════════╝\n\n"
        )
    
    @staticmethod
    def progress_bar(current: int, total: int, length: int = 20) -> str:
        """إنشاء شريط تقدم"""
        if total == 0:
            return "[░░░░░░░░░░░░░░░░░░] 0%"
        
        percentage = (current / total) * 100
        filled = int((current / total) * length)
        bar = '█' * filled + '░' * (length - filled)
        
        # اختيار اللون حسب التقدم
        if percentage >= 75:
            color = Icons.GREEN
        elif percentage >= 50:
            color = Icons.BLUE
        elif percentage >= 25:
            color = Icons.ORANGE
        else:
            color = Icons.RED
        
        return f"{color} [{bar}] {percentage:.0f}%"
    
    @staticmethod
    def timer_display(seconds: int, warning_threshold: int = 10) -> str:
        """عرض الوقت المتبقي"""
        minutes = seconds // 60
        secs = seconds % 60
        
        if seconds <= warning_threshold:
            icon = Icons.ALARM
            color = Icons.RED
        elif seconds <= warning_threshold * 2:
            icon = Icons.WARNING
            color = Icons.ORANGE
        else:
            icon = Icons.TIMER
            color = Icons.BLUE
        
        time_str = f"{minutes:02d}:{secs:02d}" if minutes > 0 else f"00:{secs:02d}"
        return f"{color} {icon} **الوقت المتبقي:** `{time_str}`"
    
    @staticmethod
    def score_display(correct: int, total: int) -> str:
        """عرض النتيجة"""
        percentage = (correct / total) * 100 if total > 0 else 0
        
        if percentage >= 90:
            emoji = Icons.TROPHY
        elif percentage >= 75:
            emoji = Icons.MEDAL
        elif percentage >= 60:
            emoji = Icons.TARGET
        else:
            emoji = Icons.BOOK
        
        return f"{emoji} **النتيجة:** {correct}/{total} ({percentage:.1f}%)"
    
    @staticmethod
    def difficulty_stars(level: str) -> str:
        """عرض مستوى الصعوبة بالنجوم"""
        levels = {
            'easy': '★★☆☆☆ سهل',
            'medium': '★★★☆☆ متوسط',
            'hard': '★★★★☆ صعب',
            'expert': '★★★★★ خبير'
        }
        return levels.get(level, '★★★☆☆')
    
    @staticmethod
    def section_divider(char: str = "═") -> str:
        """فاصل بصري"""
        return char * 36
    
    @staticmethod
    def button(text: str, callback: str, icon: str = "") -> InlineKeyboardButton:
        """إنشاء زر"""
        return InlineKeyboardButton(f"{icon} {text}", callback_data=callback)
    
    @staticmethod
    def back_button(callback: str = 'main_menu') -> List[InlineKeyboardButton]:
        """زر العودة"""
        return [InlineKeyboardButton(f"{Icons.BACK} العودة", callback_data=callback)]

# ═══════════════════════════════════════════════════════════════════════════════
# 🧠 منطق الاختبار والأسئلة
# ═══════════════════════════════════════════════════════════════════════════════

class QuizSession:
    """جلسة اختبار فردية"""
    
    def __init__(self, test_id: str, user_id: int):
        self.test_id = test_id
        self.user_id = user_id
        self.test_data = question_manager.get_test(test_id)
        self.questions = self.test_data.get('questions', []).copy()
        self.current_index = 0
        self.score = 0
        self.start_time = None
        self.timer_task = None
        self.answers = []  # تتبع الإجابات
    
    def get_current_question(self) -> Optional[Dict[str, Any]]:
        """الحصول على السؤال الحالي"""
        if 0 <= self.current_index < len(self.questions):
            return self.questions[self.current_index]
        return None
    
    def answer(self, answer: str) -> bool:
        """تسجيل إجابة"""
        question = self.get_current_question()
        if not question:
            return False
        
        is_correct = (answer == question['correct'])
        
        self.answers.append({
            'question': self.current_index,
            'user_answer': answer,
            'correct_answer': question['correct'],
            'is_correct': is_correct
        })
        
        if is_correct:
            self.score += 1
        
        self.current_index += 1
        return is_correct
    
    def is_finished(self) -> bool:
        """التحقق من انتهاء الاختبار"""
        return self.current_index >= len(self.questions)
    
    def get_results(self) -> Dict[str, Any]:
        """الحصول على النتائج"""
        total = len(self.questions)
        percentage = (self.score / total) * 100 if total > 0 else 0
        
        # تحديد المستوى
        if percentage >= 90:
            level = 'excellent'
            level_name = 'ممتاز'
            icon = Icons.TROPHY
        elif percentage >= 75:
            level = 'good'
            level_name = 'جيد جداً'
            icon = Icons.MEDAL
        elif percentage >= 60:
            level = 'pass'
            level_name = 'مقبول'
            icon = Icons.TARGET
        else:
            level = 'fail'
            level_name = 'يحتاج مراجعة'
            icon = Icons.BOOK
        
        return {
            'score': self.score,
            'total': total,
            'percentage': percentage,
            'level': level,
            'level_name': level_name,
            'icon': icon,
            'answers': self.answers
        }

# تخزين الجلسات النشطة
active_sessions = {}

# ═══════════════════════════════════════════════════════════════════════════════
# 🤖 معالجات البوت الرئيسية
# ═══════════════════════════════════════════════════════════════════════════════

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """الصفحة الرئيسية"""
    user = update.effective_user
    
    text = UI.header("بوت أسئلة العهد", Icons.BOT)
    text += (
        f"{Icons.BOOK} **مادة: أنشطة الإدارة في الإسلام**\n"
        f"{Icons.TARGET} **المستوى الأول - الترم الثاني**\n\n"
        f"{UI.section_divider()}\n\n"
        f"{Icons.GOLD} **الاختبارات المتاحة:**\n\n"
    )
    
    # عرض الاختبارات المتاحة
    tests = question_manager.get_all_tests()
    keyboard = []
    
    for test_id, test_data in tests.items():
        count = len(test_data.get('questions', []))
        text += (
            f"{test_data['icon']} **{test_data['name']}**\n"
            f"├ {test_data['description']}\n"
            f"└ {Icons.TEST} عدد الأسئلة: {count}\n\n"
        )
        keyboard.append([InlineKeyboardButton(
            f"{test_data['icon']} {test_data['name']} ({count} سؤال)",
            callback_data=f'start_test:{test_id}'
        )])
    
    text += (
        f"{UI.section_divider()}\n\n"
        f"{Icons.TIMER} **وقت الإجابة:** {TIME_LIMIT} ثانية للسؤال\n"
        f"{Icons.CHART} **المميزات:**\n"
        f"• شريط تقدم مباشر\n"
        f"• مؤقت تنازلي مع تنبيه\n"
        f"• إحصائيات تفصيلية\n"
        f"• تقييم فوري للأداء\n\n"
        f"_اختر الاختبار المناسب:_"
    )
    
    keyboard.extend([
        [InlineKeyboardButton(f"{Icons.CHART} إحصائياتي", callback_data='my_stats')],
        [InlineKeyboardButton(f"{Icons.INFO} عن البوت", callback_data='about')]
    ])
    
    await update.message.reply_text(
        text,
        reply_markup=InlineKeyboardMarkup(keyboard),
        parse_mode='Markdown'
    )

async def show_stats(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """عرض إحصائيات المستخدم"""
    query = update.callback_query
    user_id = query.from_user.id
    stats = user_stats.get_user_stats(user_id)
    
    text = UI.header("إحصائياتك الشخصية", Icons.CHART)
    
    if not stats['tests']:
        text += (
            f"{Icons.WARNING} **لم تجرِ أي اختبار بعد!**\n\n"
            f"ابدأ باختيار اختبار من القائمة الرئيسية."
        )
    else:
        text += f"{Icons.FIRE} **إجمالي المحاولات:** {stats['total_attempts']}\n\n"
        
        for test_id, test_data in stats['tests'].items():
            test_info = question_manager.get_test(test_id)
            icon = test_info.get('icon', Icons.TEST)
            name = test_info.get('name', test_id)
            
            best = test_data.get('best_score', 0)
            avg = test_data.get('average', 0)
            attempts = len(test_data.get('attempts', []))
            
            text += (
                f"{icon} **{name}**\n"
                f"├ المحاولات: {attempts}\n"
                f"├ أفضل نتيجة: {best:.1f}%\n"
                f"└ المعدل: {avg:.1f}%\n\n"
            )
        
        if stats['last_activity']:
            text += f"{Icons.TIMER} آخر نشاط: {stats['last_activity'].strftime('%Y-%m-%d %H:%M')}"
    
    keyboard = [UI.back_button()]
    await query.edit_message_text(
        text,
        reply_markup=InlineKeyboardMarkup(keyboard),
        parse_mode='Markdown'
    )

async def about_bot(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معلومات عن البوت"""
    query = update.callback_query
    
    text = UI.header("عن البوت", Icons.INFO)
    text += (
        f"{Icons.BOT} **بوت أسئلة مادة أنشطة الإدارة في الإسلام**\n\n"
        f"{Icons.BOOK} **المحتوى:**\n"
        f"• الاختبار الأول: الدروس 7-10 والتكميلية\n"
        f"• الاختبار الثاني: أسئلة العهد كاملة\n"
        f"• اختبار الفراغات: أكمل الفراغ من الأسئلة\n\n"
        f"{Icons.TARGET} **المميزات:**\n"
        f"• واجهة تفاعلية متطورة\n"
        f"• نظام مؤقت ذكي مع تحذيرات\n"
        f"• شريط تقدم مرئي\n"
        f"• إحصائيات مفصلة\n"
        f"• تقييم فوري\n\n"
        f"{Icons.LIGHT} **المصدر:**\n"
        f"عهد الإمام علي (ع) لمالك الأشتر\n"
        f"دروس السيد القائد\n\n"
        f"{Icons.HEART} **تم التطوير بعناية فائقة**"
    )
    
    keyboard = [UI.back_button()]
    await query.edit_message_text(
        text,
        reply_markup=InlineKeyboardMarkup(keyboard),
        parse_mode='Markdown'
    )

async def start_test(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """بدء اختبار جديد"""
    query = update.callback_query
    test_id = query.data.split(':')[1]
    user_id = query.from_user.id
    
    # إنشاء جلسة جديدة
    session = QuizSession(test_id, user_id)
    active_sessions[user_id] = session
    
    test_data = question_manager.get_test(test_id)
    
    text = UI.header(f"بدء {test_data['name']}", test_data['icon'])
    text += (
        f"{Icons.TEST} **عدد الأسئلة:** {len(session.questions)}\n"
        f"{Icons.TIMER} **الوقت لكل سؤال:** {TIME_LIMIT} ثانية\n"
        f"{Icons.TARGET} **نظام التقييم:** فوري\n\n"
        f"{UI.section_divider()}\n\n"
        f"{Icons.WARNING} **ملاحظات:**\n"
        f"• اضغط على الإجابة الصحيحة\n"
        f"• يوجد مؤقت تنازلي لكل سؤال\n"
        f"• سيتم تحذيرك عند {WARNING_TIME} ثوانٍ متبقية\n"
        f"• يمكنك إنهاء الاختبار في أي وقت\n\n"
        f"_اضغط 'ابدأ الآن' للبدء_"
    )
    
    keyboard = [[InlineKeyboardButton(
        f"{Icons.PLAY} ابدأ الآن", callback_data='begin_quiz'
    )]]
    
    await query.edit_message_text(
        text,
        reply_markup=InlineKeyboardMarkup(keyboard),
        parse_mode='Markdown'
    )

async def send_question(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """إرسال السؤال مع المؤقت"""
    query = update.callback_query
    user_id = query.from_user.id
    
    if user_id not in active_sessions:
        await query.edit_message_text(
            f"{Icons.ERROR} انتهت الجلسة. ابدأ من جديد.",
            reply_markup=InlineKeyboardMarkup([UI.back_button()])
        )
        return
    
    session = active_sessions[user_id]
    question = session.get_current_question()
    
    if not question:
        await end_quiz(update, context)
        return
    
    # إلغاء المؤقت السابق
    if session.timer_task:
        session.timer_task.cancel()
    
    # إنشاء مؤقت جديد
    loop = asyncio.get_event_loop()
    session.timer_task = loop.create_task(
        question_timer(update, context, user_id, session.current_index)
    )
    
    # بناء رسالة السؤال
    q_num = session.current_index + 1
    total = len(session.questions)
    
    text = UI.header(f"السؤال {q_num}/{total}", Icons.TEST)
    text += f"{question['question']}\n\n"
    
    # شريط التقدم
    progress = UI.progress_bar(q_num - 1, total)
    text += f"{progress}\n\n"
    
    # الوقت
    text += UI.timer_display(TIME_LIMIT) + "\n"
    text += f"{Icons.CHECK} **الإجابات الصحيحة:** {session.score}"
    
    # بناء أزرار الإجابة
    keyboard = []
    
    if question['type'] == 'multiple_choice':
        for i, option in enumerate(question['options']):
            opt_key = chr(65 + i)  # A, B, C, D
            keyboard.append([InlineKeyboardButton(
                option, callback_data=f'answer:{opt_key}'
            )])
    
    elif question['type'] == 'true_false':
        keyboard = [
            [InlineKeyboardButton("صحيح ✅", callback_data='answer:صحيح')],
            [InlineKeyboardButton("خطأ ❌", callback_data='answer:خطأ')]
        ]
    
    elif question['type'] == 'fill_blank':
        # للفراغات، نعرض إجابة واحدة صحيحة وبعض الخيارات الخاطئة
        # في الواقع، يمكن استخدام نظام إدخال نصي أو أزرار
        keyboard.append([InlineKeyboardButton(
            f"الإجابة: {question['correct']}", 
            callback_data=f'answer:{question["correct"]}'
        )])
    
    keyboard.append([InlineKeyboardButton(
        f"{Icons.STOP} إنهاء الاختبار", callback_data='end_quiz'
    )])
    
    try:
        await query.edit_message_text(
            text,
            reply_markup=InlineKeyboardMarkup(keyboard),
            parse_mode='Markdown'
        )
    except Exception as e:
        logging.error(f"Error sending question: {e}")

async def question_timer(update: Update, context: ContextTypes.DEFAULT_TYPE, 
                        user_id: int, question_idx: int):
    """المؤقت التنازلي للسؤال"""
    session = active_sessions.get(user_id)
    if not session:
        return
    
    query = update.callback_query
    
    for remaining in range(TIME_LIMIT, 0, -5):
        await asyncio.sleep(5)
        
        # التحقق من أننا لا زلنا على نفس السؤال
        current_session = active_sessions.get(user_id)
        if not current_session or current_session.current_index != question_idx:
            return
        
        # تحديث العرض عند التغييرات المهمة
        if remaining <= WARNING_TIME or remaining % 10 == 0:
            try:
                question = session.get_current_question()
                if not question:
                    return
                
                q_num = session.current_index + 1
                total = len(session.questions)
                
                text = UI.header(f"السؤال {q_num}/{total}", Icons.TEST)
                text += f"{question['question']}\n\n"
                text += UI.progress_bar(q_num - 1, total) + "\n\n"
                text += UI.timer_display(remaining) + "\n"
                text += f"{Icons.CHECK} **الإجابات الصحيحة:** {session.score}"
                
                # إضافة تحذير عند الوقت المنخفض
                if remaining <= WARNING_TIME:
                    text += f"\n\n{Icons.ALARM} **تنبيه: الوقت ينفذ!**"
                
                await query.edit_message_text(
                    text,
                    reply_markup=query.message.reply_markup,
                    parse_mode='Markdown'
                )
            except Exception as e:
                logging.error(f"Timer update error: {e}")
    
    # انتهاء الوقت
    await handle_timeout(update, context, user_id)

async def handle_timeout(update: Update, context: ContextTypes.DEFAULT_TYPE, user_id: int):
    """معالجة انتهاء الوقت"""
    session = active_sessions.get(user_id)
    if not session:
        return
    
    # تسجيل إجابة خاطئة (بدون إجابة)
    session.answers.append({
        'question': session.current_index,
        'user_answer': 'انتهى الوقت',
        'correct_answer': session.get_current_question()['correct'],
        'is_correct': False
    })
    session.current_index += 1
    
    query = update.callback_query
    
    await query.edit_message_text(
        f"{Icons.ALARM} **انتهى الوقت!**\n\n"
        f"_جارٍ الانتقال للسؤال التالي..._",
        parse_mode='Markdown'
    )
    
    await asyncio.sleep(1.5)
    await send_question(update, context)

async def handle_answer(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """معالجة الإجابة"""
    query = update.callback_query
    await query.answer()
    
    user_id = query.from_user.id
    
    if user_id not in active_sessions:
        return
    
    session = active_sessions[user_id]
    
    # إلغاء المؤقت
    if session.timer_task:
        session.timer_task.cancel()
    
    answer = query.data.split(':')[1]
    is_correct = session.answer(answer)
    
    # عرض التغذية الراجعة
    question = session.questions[session.current_index - 1]
    
    if is_correct:
        feedback = (
            f"{Icons.SUCCESS} **إجابة صحيحة!** {Icons.CHECK}\n\n"
        )
    else:
        feedback = (
            f"{Icons.ERROR} **إجابة خاطئة** {Icons.CROSS}\n"
            f"الإجابة الصحيحة: **{question['correct']}**\n\n"
        )
    
    # إضافة التوضيح إن وجد
    if question.get('explanation'):
        feedback += f"{Icons.LIGHT} **توضيح:** {question['explanation']}\n\n"
    
    feedback += f"_جارٍ الانتقال للسؤال التالي..._"
    
    await query.edit_message_text(feedback, parse_mode='Markdown')
    await asyncio.sleep(2)
    await send_question(update, context)

async def end_quiz(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """إنهاء الاختبار وعرض النتائج"""
    query = update.callback_query
    user_id = query.from_user.id
    
    if user_id not in active_sessions:
        await query.edit_message_text(
            f"{Icons.ERROR} لا يوجد اختبار نشط.",
            reply_markup=InlineKeyboardMarkup([UI.back_button()])
        )
        return
    
    session = active_sessions[user_id]
    
    # إلغاء المؤقت
    if session.timer_task:
        session.timer_task.cancel()
    
    results = session.get_results()
    
    # حفظ النتيجة
    user_stats.record_attempt(
        user_id, session.test_id, 
        results['score'], results['total']
    )
    
    # بناء تقرير النتائج
    text = UI.header("نتيجة الاختبار", Icons.TROPHY)
    
    # شريط التقدم النهائي
    progress = UI.progress_bar(results['score'], results['total'], 25)
    text += f"{progress}\n\n"
    
    text += (
        f"{results['icon']} **المستوى:** {results['level_name']}\n"
        f"{Icons.CHECK} **الإجابات الصحيحة:** `{results['score']}`\n"
        f"{Icons.CROSS} **الإجابات الخاطئة:** `{results['total'] - results['score']}`\n"
        f"{Icons.TARGET} **النسبة المئوية:** `{results['percentage']:.1f}%`\n\n"
    )
    
    # تقييم مفصل
    if results['level'] == 'excellent':
        text += f"{Icons.FIRE} **أداء ممتاز! استمر في التميز.**"
    elif results['level'] == 'good':
        text += f"{Icons.STAR} **أداء جيد جداً! يمكنك التحسن أكثر.**"
    elif results['level'] == 'pass':
        text += f"{Icons.WARNING} **أداء مقبول. يحتاج لمراجعة.**"
    else:
        text += f"{Icons.BOOK} **يحتاج لمراجعة شاملة. لا تيأس!**"
    
    text += f"\n\n{Icons.SAVE} **تم حفظ النتيجة في إحصائياتك**"
    
    # أزرار التنقل
    keyboard = [
        [InlineKeyboardButton(f"{Icons.CHART} إحصائياتي", callback_data='my_stats')],
        [InlineKeyboardButton(f"{Icons.REFRESH} اختبار آخر", callback_data='main_menu')]
    ]
    
    # حذف الجلسة
    del active_sessions[user_id]
    
    await query.edit_message_text(
        text,
        reply_markup=InlineKeyboardMarkup(keyboard),
        parse_mode='Markdown'
    )

async def button_router(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """موجه الأزرار الرئيسي"""
    query = update.callback_query
    await query.answer()
    
    data = query.data
    
    if data == 'main_menu':
        # إعادة بناء رسالة البداية
        await start(update, context)
    
    elif data == 'my_stats':
        await show_stats(update, context)
    
    elif data == 'about':
        await about_bot(update, context)
    
    elif data.startswith('start_test:'):
        await start_test(update, context)
    
    elif data == 'begin_quiz':
        await send_question(update, context)
    
    elif data.startswith('answer:'):
        await handle_answer(update, context)
    
    elif data == 'end_quiz':
        await end_quiz(update, context)

# ═══════════════════════════════════════════════════════════════════════════════
# 🚀 تشغيل البوت
# ═══════════════════════════════════════════════════════════════════════════════

def main():
    """الدالة الرئيسية"""
    print("\n" + "═" * 50)
    print("  🤖 بوت أسئلة العهد - النسخة الاحترافية")
    print("═" * 50)
    
    # إحصائيات
    tests = question_manager.get_all_tests()
    total_questions = sum(len(t['questions']) for t in tests.values())
    
    print(f"\n📊 إحصائيات النظام:")
    for test_id, test_data in tests.items():
        count = len(test_data['questions'])
        print(f"  {test_data['icon']} {test_data['name']}: {count} سؤال")
    print(f"\n📚 إجمالي الأسئلة: {total_questions}")
    print(f"⏱️  وقت الإجابة: {TIME_LIMIT} ثانية")
    print("\n🚀 جاري تشغيل البوت...\n")
    
    # إنشاء التطبيق
    app = Application.builder().token(TOKEN).build()
    
    # إضافة المعالجات
    app.add_handler(CommandHandler("start", start))
    app.add_handler(CallbackQueryHandler(button_router))
    
    # تشغيل البوت
    app.run_polling(allowed_updates=Update.ALL_TYPES)

if __name__ == '__main__':
    main()
